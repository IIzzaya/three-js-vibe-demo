# 项目本地配置（不入 git）
#
# 首次使用：cp .env.example .env 然后填写下方字段

# 项目名称（用于文档和显示）
PROJECT_NAME=three-js-vibe-demo

# 制作人本人名（kebab-case，全小写）。
# AI commit 身份会派生为 ${PROJECT_OWNER}-ai
# 例：PROJECT_OWNER=agent  →  AI commit author = agent-ai
#
# 缺失此项时 AI 会在 commit 前阻断并提示制作人填写。
PROJECT_OWNER=agent
