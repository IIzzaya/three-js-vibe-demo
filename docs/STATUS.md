# 项目状态全景

> 本文件由 `npm run status:rebuild` 自动生成，**禁止手工编辑**。
> 数据源：`docs/backlog/{active,completed,abandoned}/sprint-*.md` 的 frontmatter。
> 详见 `.cursor/rules/sprint-workflow.mdc`。

# 项目全景

> 本文件由 `npm run status:rebuild` 自动聚合所有 sprint frontmatter 重建，**禁止手工编辑**。
> 项目架构和约定见 [`AGENTS.md`](../AGENTS.md)；每日编码规则见 [`.cursor/rules/global.mdc`](../.cursor/rules/global.mdc)。

## 项目定位
<!-- 在此填写你的项目定位和愿景 -->
本项目是一个基于 Three.js + Express + Socket.IO 的 3D 多人联网游戏，采用 AI 驱动 Sprint 工作流开发。

## 前端入口地图
| URL | 入口 | 用途 |
| --- | --- | --- |
| `/` | `frontend/index.html` → `main.ts` | 主场景（WASD 移动 + 多人同步 + 聊天） |

## 🟢 Active Sprint

（暂无 active sprint —— 跑 `/sprint-review` 创建）
## 📦 Completed Sprint（按 track 分组）

（暂无 completed sprint）
